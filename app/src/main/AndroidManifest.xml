<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.mes_cours">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/MyNoActionBarTheme">
        <activity android:name=".activities.premieres.maths.num.ListesMathsNumActivity"></activity>
        <activity
            android:name=".activities.premieres.maths.MathsActivity"
            android:theme="@style/MySimpleDayNightTheme" />
        <activity
            android:name=".activities.premieres.ListesMatieresActivity"
            android:theme="@style/MySimpleDayNightTheme" />
        <activity android:name=".activities.MainActivity" />
        <activity android:name=".activities.SplashscreenActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>